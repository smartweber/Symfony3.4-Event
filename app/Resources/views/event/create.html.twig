{% extends 'base.html.twig' %}
{% block body %}
    <div class="container mt-3">
        <div class="row">
            <div class="col-xs-12">
                <h1 class="mb-3">New Event</h1>

                {{ form_start(form) }}
                    {{ form_widget(form) }}
                    <button type="submit" class="btn btn-primary">Save</button>
                    <a href="{{ path('event_list')}}" class="btn btn-default my-3">Cancel</a>
                {{ form_end(form) }}
            </div>
        </div>
    </div>
{% endblock %}
